# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Query {
  user: [User!]!
}

type User {
  id: ID!
  name: String!
  email: String!
}

type Mutations {
  createUser(name: String!, email: String!): User!
}
